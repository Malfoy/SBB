Place the upstream C++ benchmark executables here:

- `biobloommaker`
- `biobloomcategorizer`

The benchmark scripts default to this directory via `CPP_BIN_DIR`.
If the binaries need shared libraries, place those under `bench/lib/`
or override `CPP_LIB_DIR`.
