//! maybe ill make some unit tests based of this shit one day
//! and carefull, edd the block size of the vec to be 1

use crate::decyclers;

//use decyclers::{Decycler, compute_membership, init_vec_ci};
//use packed_seq::{Seq, PackedSeqVec, SeqVec, PackedSeq};
use decyclers::{Decycler};
use packed_seq::{Seq, PackedSeqVec, SeqVec};

///test on all the minimizer that i hand computed to check
///1000100011001110000011001100111000000000111011100000000000001111 should be the answer
pub fn _all_mins_size_3() {
    let numero: u64 = PackedSeqVec::from_ascii("AAT".as_bytes()).as_slice().as_u64();
    println!("{numero}");
    let numero: u64 = PackedSeqVec::from_ascii("TAA".as_bytes()).as_slice().as_u64();
    println!("{numero}");
    let mut small_decycler: Decycler = Decycler::new(3);
    small_decycler.compute_blocks();
    let da_entier = small_decycler.direct_list[0][0];
    let cetaitbiencequonattendait: bool = 
        da_entier == 0b1000110011101111000001001110111100000000001011110000000000000001;
    println!("c'était bien l'entier qu'on attendait : {cetaitbiencequonattendait}");
    println!("{:#b}", da_entier);

    println!("maybe the light at the end of the tunnel here");
    let mut mid_decycler: Decycler = Decycler::new(5);
    mid_decycler.compute_blocks();
    let lui_il_fait_peur: bool = mid_decycler.lookup(PackedSeqVec::from_ascii("TTTTC".as_bytes()).as_slice());
    println!("the tunnel be like {lui_il_fait_peur}");
    let lui_il_fait_peur: bool = mid_decycler.lookup(PackedSeqVec::from_ascii("CTTTT".as_bytes()).as_slice());
    println!("the tunnel be like {lui_il_fait_peur}");


    print!("0b"); //to align it with previous prints
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AAA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AAC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AAT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AAG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ACA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ACC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ACT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ACG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ATA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ATC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ATT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("ATG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AGA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AGC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AGT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("AGG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CAA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CAC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CAT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CAG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CCA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CCC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CCT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CCG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CTA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CTC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CTT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CTG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CGA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CGC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CGT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("CGG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TAA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TAC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TAT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TAG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TCA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TCC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TCT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TCG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TTA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TTC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TTT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TTG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TGA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TGC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TGT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("TGG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GAA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GAC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GAT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GAG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GCA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GCC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GCT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GCG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GTA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GTC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GTT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GTG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GGA".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GGC".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GGT".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    let lui_il_y_est: bool = small_decycler.lookup(PackedSeqVec::from_ascii("GGG".as_bytes()).as_slice());
    print!("{}", lui_il_y_est as u8);
    println!("");
}
